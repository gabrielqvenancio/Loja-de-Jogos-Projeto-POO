public class Game
{
    public string Name; //{get;}
    public DateTime ReleaseDate; //{get;}
    public int ReleasePrice; //{get; private set}
    public int SellingPriceHistory;

    // public override int GetHashCode()
    // {
    //     // Combines the values to create a single hash code
    //     //return HashCode.Combine(Name, Id);
    // }
}